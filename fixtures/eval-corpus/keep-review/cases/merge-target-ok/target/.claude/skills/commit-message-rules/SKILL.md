---
name: commit-message-rules
description: Apply the repository commit message convention. Use when writing a commit message.
---

# commit-message-rules

コミット件名は `<type>: <要約>`（type は feat/fix/docs/chore）。本文は72桁で折り返す。
破壊的変更は `BREAKING CHANGE:` を本文先頭に置く。
