# canon-issues-20260919_023121 / 20260922_172924 対処記録

`design/canon-issues-20260919_023121.md`（本編9件: S1:2・S2:3・S3:4）と
`design/canon-issues-20260922_172924.md`（本編10件: S1:3・S2:4・S3:3。うち再発4件）の計19件を、
5時間枠対策の自己改修（`design/canon-optimization-20260924.md`）と合わせて対処した。件数は両文書の
`## ` 見出しを数え直した値（参考節は含まない）。**対処済み17件・未対応2件**。
`npm test` は tests 582 / pass 581 / fail 1（既存の OS 依存の失敗1件・下記末尾）。

判定は「コード・定義・テストで裏取りできたもの」だけを対処済みとした。根拠のコミットは
489c153・bb722f3・9295c5a・d13b458・b644fdc・6482a15（いずれも main）。

---

## run 20260919_023121

### S1-1（deploy.js の step1 が try の外で半壊状態を残す） — 対処済み

- `deploy/deploy.js`（6482a15）: step0 に事前検査 `probeMovable` を置き、退避する全ファイルを同じ場所で
  rename して戻せるかを確かめ、1件でも動かせなければ**対象を変更せずに**拒否する（推奨2）。step1 を try の内側へ
  移し、復元は実際に退避・配置したファイルだけを対象にする（推奨1）。
- 裏取りの過程で、旧 restore が `managed-paths.list` の全エントリを削除する作りで、step1 の途中失敗では
  **まだ退避していない元ファイルを消しうる**ことを見つけ、変異注入で再現してテストで固定した（issue 文書に無い追加の欠陥）。
- 終了メッセージは「対象は一切変更していない（事前検査で拒否）」「配置前状態へ復帰した（restored）」
  「復帰が完了していない（partial・.bak を残す）」を区別して出す（推奨3）。
- `/canon` SKILL.md 工程10 と RUN.md（`deploy/emit-run-manifest.js`）に「`--confirm` はサンドボックスの外で実行」を明記（推奨4）。
- 実地検証: `tests/deploy_swap.test.js` の「deploy 事前検査（EBUSY 注入）」「deploy step1 の途中失敗（EBUSY 注入・事前検査をすり抜けた場合）」
  「deploy step1 の後半で失敗」「deploy 復元の失敗」「probeMovable: 動かせるものは元に戻して何も変えない」（推奨5）。

### S1-2（agent-builder の `</content>` 残骸を G12 が検出しない） — **未対応**

- 現状: `gates/` にツール呼び出しの書式片（`</content>`・`</parameter>`・`<parameter name=`）を行単位で検出する
  検査は無い（`grep` で確認）。agent-builder 定義にも注意書きは無い。
- スコープ外にした理由: 今回の改修は「コスト（5時間枠）と、記録済みのまま再発した欠陥」を優先するというユーザー方針で
  範囲を切った。本件は run 20260919 で1回観測され、run 20260922 では再発の記録が無い。
- 次の一手（issue 文書の推奨の要約）: G12 に「コードフェンス外で書式片が行単位で現れたら違反」を加え、本文末尾・frontmatter 直後・
  コードフェンス内の3位置に注入して前2者だけが違反になる回帰テストを置く。builder 定義への注意書きは補助に留める。

### S2-1（investigator 配下の報告がメインへ配送される） — 対処済み

- bb722f3 で `investigator` を廃止し、系統A `existing-customization-analyzer`・系統B `project-profiler` を
  メイン Claude が同一 turn で並列に直接 spawn する形にした。各ワーカーは自分の成果物を自分で書く
  （`work/<ts>/existing_customizations.md`・`project_profile.md`）ので、報告の配送先に依存せず、逐語転記も不要になった
  （推奨1。推奨2 の転記 diff 検証は、転記そのものが無くなったため不要）。
- 実地検証: `tests/self_application.test.js`「investigator（深さ2の中継役）は廃止済み。系統A/B は自分の成果物を書ける」、
  `tests/stage_guard_investigation.test.js`「F1 ④ existing_customizations.md 不在」。

### S2-2（eval:bundle がバンドルディレクトリを掃除しない） — 対処済み

- `eval/bundle.js`（b644fdc）: keep-review バンドルの出力先を書き出し前に掃除する。round 1 では `eval-bundle/` 全体と
  前の試行の `eval/*.md`・`eval-report.md` も消す（`eval/round.js` `startFirstRound`）。
- 推奨の後半（`eval:report` のカバレッジ検査に「バンドルのファイル集合 = design-map の keep/merge 集合」を加える）は**入れていない**
  （`eval/report.js` はバンドルを読まない）。掃除により残骸自体が生じないことで代えた。
- 実地検証: `tests/eval_bundle.test.js`「S2-2 掃除: 前 round のバンドルは書き出し前に消える」、
  `tests/eval_round.test.js`「round 1: 前の試行の判定（eval/・eval-report.md）と eval-bundle/ を消し…」。

### S2-3（Bash を持たない generator にテスト実測を要求した） — 対処済み

- `/canon` SKILL.md 工程8（6482a15）に「実行を要する検証はオーケストレータ（メイン Claude）が行い、結果を P6+7 の提示に含める。
  実行できなければ『実行して確かめていない』と明示する」を追加。`layer-design` Skill にも「検証の主体をオーケストレータと書く」制約を追加。
- 定義文の変更のみで、機械検査・テストは無い。

### S3-1（recheck が focused 追記後に誤警告する） — 対処済み

- 9295c5a: `tools/recheck.js` の鮮度判定を `gates/lib/run-status.js` へ移し、`investigation.focused` マーカーがある間は
  `project_profile.md` の追記を focused 側の管轄として扱う。
- 実地検証: `tests/run_status.test.js`「S3-1 鮮度: focused マーカーがある間、project_profile.md の追記で調査1のマーカーを『古い』と誤警告しない」。

### S3-2（`node -e` の書込先をガードが特定できない） — **未対応**

- 現状: `/canon` SKILL.md の契約5（一時ファイル）に「インタプリタの `-e` 内の書込は宛先を同定できず封鎖される。書込はリダイレクトで行う」
  旨の記述は無い（`grep` で確認）。ガードの挙動（同定不能は広域スキャンへフォールバック）は変えていない。
- スコープ外にした理由: S1-2 と同じ（コストと再発を優先）。issue 文書自身が「挙動自体は安全側で妥当」としており、
  品質に影響しない S3 である。なお、調査ワーカーが自分の成果物を書く形（S2-1）になり、本件を踏んだ代筆作業そのものは
  通常経路から消えた。
- 次の一手（推奨の要約）: SKILL.md 契約5 に上記1文を追記する（ガードは緩めない）。

### S3-3（サンドボックスのマウントポイントが git status に現れる） — 対処済み（**未コミット**）

- `/canon` SKILL.md「run の終い方」に「コミット時は `git add <パス>` で対象を明示する。`git add -A`・`git add .` は
  マウントポイントの未追跡ファイルを拾う」を追記した。
- **この変更は作業ツリーにあるが、本記録の作成時点ではコミットされていない**（`git diff .claude/skills/canon/SKILL.md` の1行）。
  canon 側で対処できる範囲は注意書きのみ（ハーネス挙動）。

### S3-4（配置後の push で対象側の sandbox 例外が効かない） — 対処済み

- 6482a15: RUN.md の「実行環境の注意」と SKILL.md 工程10 に「対象リポジトリへの push は対象リポジトリで起動したセッションで行う」を明記。
- 実地検証: `tests/deploy_run_manifest.test.js`「RUN.md に実行環境の注意（サンドボックスの外で --confirm・push は対象のセッションで・SSH ホスト）が入る」。

### 参考（スコープ外の提案・1件）

- 陳腐化した憲法条文の参照（`ref_resolution` で `resolved: false`）を spec の是正候補へ自動で上げる仕組み: canon 本体の機能追加であり今回は扱っていない。

---

## run 20260922_172924

### R-S1-1（deploy.js 半壊の再発） — 対処済み

- 上記 S1-1 と同一の対処（6482a15）。issue 文書が「手順書1行で防げた」とした推奨4（`--confirm` はサンドボックスの外）も
  RUN.md・SKILL.md に入った。

### S1-3（MANIFEST と generated/ が機械照合されない） — 対処済み

- d13b458: MANIFEST に `## 全ファイル` 節（`generated/` の全ファイルを1行1件）を必須化し、`gates/lib/manifest.js` がパース、
  `gates/g9_snapshot_completeness.js` が `generated/` の実ファイル集合と**双方向**に照合する（推奨1）。generator 定義に節の書式と数え直しを明記。
- 推奨2（`managed-paths.list` を含む3者一致）は完全には入れていない。G9 は従来から「managed-paths.list の列挙が generated/ に実在する」
  片方向を見ており、今回 MANIFEST⇔generated/ の双方向が加わった。managed-paths.list⇔MANIFEST の直接照合は無い。
- 実地検証: `tests/g9_g10_g12.test.js`「G9 MANIFEST: 全ファイル節が無ければ違反」「G9 MANIFEST（違反注入）: 実ファイルが1件 MANIFEST に載っていなければ検出する」
  「G9 MANIFEST（違反注入）: MANIFEST に実在しないファイルが載っていれば検出する」（推奨3）。

### S1-4（builder への委譲でファイルが脱落してもゲートが検出しない） — 対処済み

- d13b458: G9 が design-map の宣言（`gates/lib/design-map.js` `listDeclaredArtifacts`・層ごとの節の見出しと keep/modify レコード）を照合元にし、
  `generated/` への全件実在を検査する（推奨1。照合元は generator の出力でなく design-map）。
- generator 定義に「builder を spawn する前に `slices/targets-<層>.txt` の件数と生成させるファイル数を突き合わせる」を明記（推奨2）。
- 実地検証: `tests/g9_g10_g12.test.js`「G9 design-map（違反注入）: 宣言した成果物が generated/ に無ければ検出し、retire 注記と実在するものは要求しない」と対照
  「G9 design-map: 宣言がすべて実在すれば通過」。コミットメッセージは実 run 20260919 の成果物で宣言32件＝生成32件・誤検知なしを確認したとしている
  （本記録の作成時には再実行していない）。

### R-S2-1（investigator の報告喪失・SendMessage 再開も不可） — 対処済み

- 上記 S2-1 と同一（bb722f3）。issue 文書の推奨「深さ2の investigator を諦め、系統A・B を直接 spawn して直接永続化する」をそのまま採った。
  あわせて差し戻しでも `SendMessage` での再開を使わず、新規 spawn＋修正指示ファイル注入に統一した（9295c5a）。

### S2-4（系統A がサマリの件数を誤って要約した） — 対処済み

- bb722f3: `existing-customization-analyzer` 定義に「サマリの件数は全レコードを書き終えてから本文の `- path:` 行を数えて書く。数えられなければ書かない」を明記（推奨1）。
  `gates/g1_stage_order.js` の investigation 段に、総数と総数行の `L<n> <件数>` 内訳を本文のレコード数と照合する検査を追加（推奨2）。
  `/canon` SKILL.md 工程1 に「系統A のサマリ総数を数え直す」手順を追加（推奨3）。
- 実地検証: `tests/g1_g7.test.js`「件数照合（違反注入）: 実際は3件なのに総数を2と書けば検出する」「件数照合（違反注入）: 総数は合っていてもレイヤー内訳が本文と違えば検出する」
  「件数照合: 総数行が無ければ照合しない」「件数照合が G1 の investigation ステージで実際に発火する」ほか。

### R-S2-2（eval:bundle の残骸の再発） — 対処済み

- 上記 S2-2 と同一（b644fdc）。

### S2-5（keep-review judge が不在を根拠に事実と異なる violation を出す） — 対処済み（推奨3 は未実施）

- b644fdc: `eval/bundle.js` が keep 対象を名指しする生成物の箇所を `file:line` で逆引きしてバンドルに同梱する（designer の主張は引き続き除去・推奨1）。
  言及が無ければ「Grep で確かめてから不在と言う」旨を出す。`quality-checklist` に「『存在しない』『言及が無い』を、探す前に根拠にする」を NG として追加（推奨2）。
- 推奨3（SKILL.md 工程9 に「violation の根拠に『存在しない』が含まれる場合はオーケストレータが横断 grep で裏取りする」）は SKILL.md に見当たらず、**未実施**。
- 実地検証: `tests/eval_bundle.test.js`「S2-5 逆引き: keep 対象を名指しする生成物の箇所が file:line で入る」「S2-5 逆引き: 言及が無ければ『Grep で確かめてから不在と言う』旨を出す」。

### R-S3-4（push の失敗の再発・SSH ホスト） — 対処済み

- 上記 S3-4 と同一（6482a15）。RUN.md に「SSH リモートなら `github.com:22` の許可も要る」を併記した。

### S3-5（run 中は design/ に書けず、reopen の警告が紛らわしい） — 対処済み

- `tools/reopen.js` の出力に「終端マーカー `generation.done` が無いあいだは run 中で保守編集は deny、鋳造後は run の外。巻き戻した直後は run 中に戻っている」
  の区別を明記（bb722f3・推奨1）。
- `/canon` SKILL.md に「run の終い方」節を新設（9295c5a・推奨2）: run が終わる時点・区間の合間はガードが武装したままであること・`design/` へ書けるのは
  `generation.done` 以降であること。
- 定義文と CLI の出力文の変更で、本件専用のテストは無い。

### S3-6（案内された .bak が実在しなかった） — 対処済み

- 6482a15: 成功時の案内は `.bak` の実在（`bakExists`）と退避件数（`baked`）を確かめてから出し、退避0件なら「.bak は作られていない。戻すなら git revert のみ」と出す（推奨1）。
  `deploy-result.json` に `bak_dir`・`bak_exists`・`baked` を記録する（推奨2）。
- 実地検証: `tests/deploy_swap.test.js`「S3-6: 対象に管理ファイルが無ければ退避は0件で .bak は作られない」「S3-6: 既存改修の成功は .bak の実在と退避件数を返す」（推奨3）。
- 観測された症状（2回目の `--confirm` 後に .bak が無かった）の**原因そのものは特定していない**。退避0件の経路は症状と整合する1つの経路であり、
  そこでの誤案内を塞いだ。

### 参考（スコープ外の提案・2件）

- spec の mandatory 節（designer の優先順位判断の対象外にする必達項目）: canon 本体の機能候補であり今回は扱っていない。
- 行数・件数を受け入れ基準の合否条件に使わない制約（spec-writer）: 同上。

---

## 既存の失敗1件（本件と無関係）

`npm test` の fail 1件は `tests/artifact.test.js` の `skillPathRole`「Windows のバックスラッシュ区切りも判定できる」で、
Linux（WSL）で実行した場合に落ちる OS 依存の既存の失敗である。今回の改修コミットでは触れていない。
